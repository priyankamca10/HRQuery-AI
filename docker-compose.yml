services:

  ollama:
    image: ollama/ollama
    container_name: ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama

  api:
    build: .
    container_name: fastapi-genai
    ports:
      - "8000:8000"
    depends_on:
      - ollama

volumes:
  ollama_data: